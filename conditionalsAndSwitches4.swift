// We have two variables age and money. Write code to determine if you can buy a car that costs 20000. For this you’ll need at least 20000 money and at least an age of 18.
var age = 18
var cash = 210000
if age >= 18 && money >= 20000 {
    print("you can buy a car")
}else{
    print("sorry")
}

// A. Write a conditional statement that prints out whether a number is a whole number.

// B. Then using a switch statement. 

let testNum: Double = 3.6
let numAsInt = Int(testNum)
let numConvertedBackToDouble == Double(numAsInt)
numConvertedBackToDouble == testNum
